<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="realease.zitatl.iste">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="Quotes"
        android:supportsRtl="true"
        android:theme="@style/Theme.RealeaseZitatListeWirklich">
        <activity android:name="realease.zitatl.iste.Hilfe" />
        <activity android:name="realease.zitatl.iste.Spiel" />
        <activity android:name="realease.zitatl.iste.bearbeiten" />
        <activity android:name="realease.zitatl.iste.Hinzufuegen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>